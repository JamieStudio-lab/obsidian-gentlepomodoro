# CLAUDE.md

Project context for Claude Code sessions in this directory. Keep this file scannable in under 60 seconds.

## Project Summary

**Gentle Pomodoro** is an Obsidian plugin: a soothing, task-integrated Pomodoro timer that links sessions to Tasks-plugin markdown items and writes Dataview-compatible daily logs. Currently **v0.4.4 (beta)**.

**0.4.4 added:** an **estimated end time** on the timer. While a session runs, `.gp-end-time` (new div in `.gp-timer-content`, built right after `.gp-mode-label` in [GentlePomoView.ts](GentlePomoView.ts) `onOpen`) shows the projected wall-clock finish, e.g. `Ends 15:30`. Unlike the deliberately-hidden countdown (`.gp-state-running .gp-timer-time { opacity: 0 }`), it stays **visible while running** — it's a static fact, not a ticking clock, so it fits the "gentle" no-countdown design. Computed **view-side** in `applySettings()` as `formatEndTime(Date.now() + state.remainingMs)` — this reconstructs the engine's private `targetTime` (sub-second jitter, invisible at minute precision), so there's **no `TimerEngine` change**. `formatEndTime` uses moment's locale-aware `LT` format and appends `(+1 day)` / `(+N days)` when the end lands on a later **calendar** day (`end.startOf("day").diff(moment().startOf("day"), "days")` — compared on local-midnight boundaries so it's DST-safe, not 24h chunks). Shown only when `showEndTime && isRunning && remainingMs > 0` (hidden on pause / stop / overtime); the DOM write is gated by a new `lastEndText` guard (mirrors `lastTimeText`), so it writes ~once/minute — no frosted-glass backdrop reflow. Handled inside `applySettings()` (which the tick listener already calls every tick) rather than the listener body, so the settings toggle live-updates open views via `applySettingsToOpenViews()`; `applySettings` now hoists its `state` lookup to the top and reuses it for both the end-time block and the day/night icon (one `const state`). New `showEndTime` boolean in `GentlePomoSettings` ([types.ts](types.ts)) + `DEFAULT_SETTINGS` ([constants.ts](constants.ts), `true` — existing users get it via the normal `Object.assign` merge, no migration). A **Show estimated end time** toggle was added to **both** the declarative `getSettingDefinitions()` and the imperative `display()` fallback in [GentlePomoSettingTab.ts](GentlePomoSettingTab.ts) (in the Timer appearance group, after Day/night indicator — keep both paths in sync). Required widening the moment stub in [momentTypes.ts](momentTypes.ts): factory input `string` → `string | number`, and `diff(other, unit?)` gains the optional `unit` — both additive (existing single-arg `diff` calls in [logManager.ts](logManager.ts) unaffected). New `.gp-end-time` CSS (right after `.gp-mode-label` in [styles.css](styles.css)) is white-with-shadow like the mode label so it reads on both the classic gradient and the frosted pane. The view toggles a **`.gp-visible`** class (not `.gp-hidden` — `display:none` can't animate), so the label **animates** in/out: a gentle fade + upward slide + `max-height` grow (~260ms), mirroring `.gp-total-time`'s recipe. Because `.gp-timer-visual` is a fixed square (`aspect-ratio: 1/1`), the reveal grows _inside_ it — only the mode label re-centers slightly, the controls below don't move. Reduced-motion users get an instant snap (`.gp-end-time` added to the `prefers-reduced-motion` `transition: none` / `transform: none` block). Also **reorganized the settings tab**: the timer-visual settings (**Theme**, **Day/night indicator**, **Show estimated end time**) are split out of "Display & behavior" into a new **Timer appearance** `.setHeading()` group, and the tab's section order is now **Display & behavior → Timer appearance → Long break → Daily focus goal → Task selector → Task integration** (task groups adjacent, timing groups adjacent; "Display & behavior" keeps logs folder / auto-open / status bar). Updated in both the declarative `getSettingDefinitions()` and imperative `display()` paths — **keep their section order in sync**.

**0.4.3 added:** redesigned the **task-picker empty state**. When `loadTasks()` in [GentlePomoView.ts](GentlePomoView.ts) finds no task groups it now renders a structured `gp-task-empty` block — muted `calendar-check` icon (via `setIcon`), an "All clear" title, and a hint `` `No tasks scheduled or due in the next ${taskSelectorDays} days.` `` — replacing the old single `gp-task-item-empty` div, which had **no CSS rule at all** (rendered as bare, unpadded plain text). New rules (`.gp-task-empty` / `-icon` / `-title` / `-hint`) sit at the end of the Task List Dropdown section in [styles.css](styles.css); the icon svg carries the same `min-width`/`min-height` + `flex-shrink: 0` floor as the control glyphs (WebKit flex-SVG sliver bug — see Important Quirks). Wording is "scheduled or due" because `loadTasks` filters on ⏳/📅 dates and overdue always passes, so an empty list truly means nothing dated in the window. **Two variants**, branched on `tasksPath.trim() !== ""`: path configured → calm all-clear (above); no path → setup nudge (`folder-search` icon, "Nothing here yet", hint to set a Tasks folder path in the plugin settings). An empty path scans the whole vault, so the nudge technically also shows for deliberate whole-vault users with zero dated open tasks — accepted tradeoff (never-configured users are the common case, and the nudge is harmless). No settings or schema changes.

**0.4.2 added:** a **Task lookahead window** dropdown in the plugin settings tab ([GentlePomoSettingTab.ts](GentlePomoSettingTab.ts), placed right after "Show task selector") — lets users pick how many days ahead the side-panel task selector reaches: **3 / 5 / 7 / 14 / 30 days** (previously hardcoded to 3). New `taskSelectorDays` number in `GentlePomoSettings` ([types.ts](types.ts)) + `DEFAULT_SETTINGS` ([constants.ts](constants.ts), `3` — preserves prior behavior; existing users get it via the normal `Object.assign` merge, no migration). Wired in `GentlePomoView.loadTasks()` — the `fetchTasks` call now passes `limitDays: settings.taskSelectorDays` (it previously omitted it, so `loadTasks`'s `limitDays = 3` default in [taskLoader.ts](taskLoader.ts) applied), and the empty-state text is now `` `No tasks found for the next ${days} days.` ``. **Overdue guarantee unchanged:** `loadTasks` filters `dateObj.isSameOrBefore(limitDate)`, so past-dated (overdue/after-scheduled) tasks always pass regardless of the window; grouping stays via `groupTasksByDate`. The picker re-fetches on each open, so no live-refresh wiring is needed — the dropdown's `onChange` just saves. Also **reorganized the settings tab** with two new `.setHeading()` rows — **Task selector** (before "Tasks folder path", grouping the three task settings) and **Display & behavior** (before "Pomodoro logs folder", bounding logs/auto-open/status-bar/day-night/theme; **0.4.4** later split day/night + theme into a separate **Timer appearance** group) — matching the existing Long break / Daily focus goal / Task integration heading pattern. (Note: the `obsidianmd/settings-tab/no-problematic-settings-headings` lint rule forbids a generic "General" heading, hence "Display & behavior".) Also **adopted Obsidian 1.13's declarative settings API** (prompted by an Obsidian review warning): `GentlePomoSettingTab` now implements `getSettingDefinitions()` (all 13 settings in the same five groups) plus `getControlValue()`/`setControlValue()` overrides that preserve every side effect (unlink-on-hide, `setStatusBarVisibility`, `applySettingsToOpenViews`, numeric guards; `taskSelectorDays` converts number↔string for the dropdown) — this makes settings show up in 1.13's settings search. The imperative `display()` is kept unchanged as the pre-1.13 fallback (never called on 1.13+), so `minAppVersion` stays 1.7.2 — **keep both paths in sync when changing settings**. Required bumping the `obsidian` types dep to `^1.13.1`, which in turn required `override` on `settings` in [main.ts](main.ts) (the 1.13 `Plugin` base class now declares `settings?: unknown`).

**0.4.1 added:** a **Show task selector** toggle in the plugin settings tab ([GentlePomoSettingTab.ts](GentlePomoSettingTab.ts), placed right after "Tasks folder path"). New `showTaskSelector` boolean in `GentlePomoSettings` ([types.ts](types.ts)) + `DEFAULT_SETTINGS` ([constants.ts](constants.ts), `true`). **Smart initial default:** `loadSettings()` in [main.ts](main.ts) derives the value once when the field is absent (fresh install or upgrade) — `showTaskSelector = tasksPath.trim() !== ""` (hidden when no tasks path, shown when one exists) — and persists via the existing `migrated` one-time-save path (same pattern as the `sunset→classic` theme migration); the user's explicit toggle wins on all later loads. **Visibility** is applied in `GentlePomoView.applySettings()` — a new `taskSelectorRow` field (ROW 3) plus `taskListContainer` get `toggleClass("gp-hidden", !showSelector)` (idempotent, runs every tick; reuses the existing `.gp-hidden` rule). **Unlink-on-hide:** the settings toggle's `onChange` calls `timer.setTask(NO_TASK_LABEL)` when switching off (a one-time state change — not done in `applySettings`, which would fire every tick; a task is never pre-linked on load since `currentTaskName` isn't persisted), then `applySettingsToOpenViews()` live-updates open panels.

**0.3.2 added:** mobile touch refinements. **Tap-to-peek auto-hide:** on touch the running countdown is revealed by tapping the timer shape, but there was no hover-out to re-hide it — and iOS _sticks_ the `:hover` state on a tapped element, which pinned it open. The `click` handler in [GentlePomoView.ts](GentlePomoView.ts) `onOpen` now _adds_ `gp-peek` and starts a `window.setTimeout` (`PEEK_REVEAL_MS` = 2000ms in [constants.ts](constants.ts)) that removes it — each tap clears/restarts the timer (reveal-and-extend, never hides early), and `peekTimeout` is cleared in `onClose`. Crucially, the two `.gp-timer-visual:hover` reveals in [styles.css](styles.css) are now wrapped in `@media (hover: hover)` so the iOS sticky-hover can't override the auto-hide; touch reveals only via the (auto-hiding) `.gp-peek` class under `@media (hover: none) and (pointer: coarse)`. **~1Hz visual throttle (iPhone flicker fix):** the engine ticks every 50ms, so the view listener ran ~20×/sec; on the frosted-glass theme that re-drove the orbs' `color-mix()` (via `--gp-progress`) under the `.gp-glass-pane` `backdrop-filter`, forcing a ~20Hz re-blur that flickered on iPhone (iPad coped). The listener now gates the countdown `setText` + the three gradient `setProperty` writes (`--gp-dusk-opacity` / `--gp-night-opacity` / `--gp-progress`) on the displayed second changing (`lastTimeText` guard), cutting them to ~1/sec; the 0.8s CSS transitions bridge the steps, so it's visually identical. Discrete state (running/mode/overtime classes, mode + task labels) still updates every tick. **Bigger, auto-shrinking touch targets:** token-driven in the Mobile & touch section of [styles.css](styles.css) — `--gp-tap-min` 44→50px (cascades to segmented control, reset button, settings rows, number inputs), and on `.gp-controls-row` `--gp-icon-btn-size` 48→56px, `--gp-icon-svg-size` 32→36px, `--gp-control-gap` 14px. The control row is `flex-wrap: nowrap` on mobile and the icon buttons are `flex: 0 1 var(--gp-icon-btn-size)` with `min-width: 44px; max-width: 56px; aspect-ratio: 1 / 1` — so they shrink to fit a narrow iPad sidebar (down to the 44px floor) instead of wrapping to two rows, and stay 56px on a full-width iPhone. The SVG `min-width` floor (36px) still protects the glyph from the WebKit sliver bug (only the button shrinks now, 44 > 36). The two-button wrappers' (`.gp-adjust-wrapper`, `.gp-secondary-controls`) `max-width` is `calc(2 * var(--gp-icon-btn-size) + var(--gp-control-gap) + 4px)` on the **base** rule, so `.gp-animated-wrapper.gp-hidden-animated { max-width: 0 }` (specificity 0,2,0 beats base 0,1,0) still collapses them for the show/hide animation. `body.is-tablet .gp-root` trims side padding to 12px (iPhone keeps 20px). Task-picker button (`.gp-btn-full`) → 60px min-height; task-list rows → 14px padding. **iOS frosted-glass compositing:** two follow-ups after device testing. (1) `.gp-timer-time` gets `transform: translateZ(0)` (+ `backface-visibility: hidden`) on mobile so its `opacity` peek-fade composites on its own GPU layer — otherwise iOS shares the fade with the frosted `backdrop-filter`'s repaints + the ~1Hz ancestor recalcs and intermittently drops the 0.3s transition (abrupt hide) and glitches the downward `text-shadow` at the digits' bottom. (2) On mobile (iPhone + iPad) + frosted glass the running-state `gp-gentle-pulse` (which `scale()`s `.gp-timer-shape`, re-rasterizing the `border-radius`+`overflow:hidden` backdrop-filter edge every frame → inner-edge blink, constant on iPhone and occasional on iPad) is swapped for `gp-gentle-pulse-glow` — a box-shadow-only breath (the drop shadow is _outside_ the clip, so it never touches the backdrop edge). Scoped `body.is-mobile/.is-tablet .gp-theme-frosted-glass` (desktop keeps the size-pulse; classic keeps its pulse) and wrapped in `@media (prefers-reduced-motion: no-preference)` because the selector (0,4,1) out-specifies the reduced-motion `animation: none` (0,2,0) and would otherwise re-enable motion.

**0.3.1 added:** mobile side-panel **scrolling & layout** fixes (all CSS, scoped to `body.is-mobile`/`body.is-tablet` in the Mobile & touch section of [styles.css](styles.css)). **Single scroll surface:** the task list and open settings panel no longer cap their height or scroll internally on mobile — `.gp-root` is the only scroller, killing the nested-scroll "find the right place to drag" trap (`transition: none` on those two on mobile, since an uncapped `max-height` can't animate, so open/close is instant there). **Timer shrink:** `.gp-timer-visual` max-width is `min(280px, 38vh)` on mobile so the sticky clock stops dominating short/landscape screens. **Short-panel timer (iPhone landscape):** [GentlePomoView.ts](GentlePomoView.ts) runs a `ResizeObserver` on `containerEl` (`.gp-root`) and toggles a `gp-compact` class when `Platform.isMobile && clientHeight < 480`; CSS (`body.is-mobile .gp-root.gp-compact .gp-timer-visual`) then sets `position: static` + `max-width: 150px` so the timer shrinks and scrolls away instead of covering the controls under its opaque sticky background. **Detection is JS-measured, not media-query-based** — Obsidian's mobile webview does NOT expose reliable viewport media queries for this view (both `@media (max-height)` and `@media (orientation)` were tried and neither engaged on iPhone rotation). The 480px height floor + `Platform.isMobile` gate leaves iPad, portrait phones, and desktop untouched. **In-view goal meter on mobile:** the `gp-goal-progress` "Today X / Y" line is now driven by the view's own timer subscription + a push on `onOpen` (new `GentlePomoPlugin.refreshViewGoalProgress(view, state?)` + extracted `focusGoalText(state)` helper in [main.ts](main.ts)), instead of only being mirrored from inside `updateStatusBar`. The old path raced the view's creation (worked on iPad, blank on iPhone) and needed the status bar to exist; the meter now renders deterministically on mobile and even when "Show in status bar" is off. **Centering:** `.gp-root` switches `scrollbar-gutter` from `stable` to `auto` on mobile (the reserved gutter shifted content off-centre on touch), and gets `flex: 1 1 auto; min-height: 0` (fills the mobile leaf) plus `justify-content: safe center` (centers the column). `.gp-root` _is_ the leaf-content element (the view empties `containerEl` and uses it directly), and its `height: 100%` doesn't resolve on mobile, so it collapsed to content height; the `::before/::after` spacer trick that centers on desktop is unreliable on mobile because Obsidian styles the leaf-content's own `::after`, so the spacers are dropped (`content: none`) on mobile and native `safe center` does the centering (top-align + scroll fallback when content exceeds the leaf). Plus `overscroll-behavior-y: contain` (no app-shell bounce-through), `overflow-x: hidden` (no sideways drift), and `padding-bottom: max(20px, env(safe-area-inset-bottom))` (clears the iPhone home indicator).

**0.3.0 added:** systematic **mobile (iPad / phone) support**. All mobile/touch CSS now lives in one consolidated `/* ===== Mobile & touch ===== */` section at the end of [styles.css](styles.css), driven by sizing tokens (`--gp-tap-min`, `--gp-icon-*-size`). Two query strategies are used deliberately: `body.is-mobile` / `body.is-tablet` for layout/sizing, `@media (hover: none) and (pointer: coarse)` for input-capability fixes. **Tap-to-peek**: the running countdown is hidden by design and revealed by hovering on desktop; on touch a tap on the timer shape toggles a `gp-peek` class (handler in [GentlePomoView.ts](GentlePomoView.ts) `onOpen`). **In-view daily-goal progress**: a new `gp-goal-progress` element (`GentlePomoView.setGoalProgress()`, fed from `main.ts` `updateStatusBar` reusing its `totalText`/`goalMet`) surfaces the goal on mobile, where Obsidian hides the status bar. Bigger touch targets (≥44px) and a control-row reflow (no more forced horizontal scroll) on mobile. Three task-picker `.onclick` handlers converted to `registerDomEvent`. **Control-glyph sizing**: a `min-width`/`min-height` floor on `.gp-icon-btn svg.svg-icon` in the Mobile & touch CSS section — a workaround for a WebKit/Safari flexbox bug that collapses an SVG flex item's main-axis width to ~8px on iPad (see Important Quirks). **Daily-log robustness**: `writeLog` now resolves the file via the Vault index with an adapter-append fallback (no silent drops under mobile/sync index lag), wraps the write in try/catch + `Notice`, and tolerates a folder-create race; log-folder scans use boundary-safe `isPathInFolder` (from [taskLoader.ts](taskLoader.ts)) instead of `path.startsWith`. Known limitation: iOS silent switch mutes WebAudio (platform constraint).

**0.2.1 fixed:** the timer now detects natural completion — when a session hits zero with the relevant auto-start toggle on, it plays the end cue (bell/ding) and auto-starts the next session (previously it just slid into overtime and never advanced). New `completeNaturally()` in `TimerEngine.startLoop()` fires once on the zero-crossing tick. Manual button semantics split: **Stop** (`finish()`) always switches to the next mode **paused** (`handleFinished(autoStartNext=false)`); **Skip** (`skip()`) respects the auto-start toggle. Audio robustness: a single shared `AudioContext` (resumed when suspended) instead of one-per-call, decoded clips cached in `audioBuffers`, and `TimerEngine.dispose()` (called from `main.ts` `onunload`) closes the context + clears the tick loop so nothing leaks across disable/enable cycles.

**0.2.0 added:** new **Frosted Glass** theme — a 3D frosted pane in front of three drifting, hue-shifting color orbs (warm sunrise/fireplace → cool twilight, driven by the `--gp-progress` CSS variable). Theme picker (Classic / Frosted glass) in the main Obsidian Settings tab. Renamed the original theme internally from `"sunset"` to `"classic"` with one-time auto-migration in `loadSettings()`. Pastel-twilight palette in Obsidian light mode; fireplace-orange warmth in dark mode with mode-specific overrides. Overtime text contrast fixes for both modes (focus and break). Smooth color interpolation on skip/reset via `@property --gp-progress` registration. `THEMES.md` documents four future theme ideas.

**0.1.0 added:** long break after every Nth focus session (classic Pomodoro Technique), daily focus goal with status-bar progress + once-per-day "goal hit" notice, opt-in `🍅 N` counter written back to task lines (lifetime total per task; never resets), volume slider and auto-start toggles in the in-view settings panel.

**0.1.1 added:** audio cues now bundled into `main.js` so catalog/BRAT installs get sound out of the box (no mp3 assets needed on disk), `prefers-reduced-motion` respect on the timer-shape animations, sigstore artifact attestations on release assets, committed `package-lock.json` + CI uses `npm ci` for reproducible builds.

**0.1.2 added:** in-view settings panel redesigned with iOS-style toggle switches, Low/Mid/High segmented volume control, section headers (Timing / Audio / Auto-start), full-width Reset to defaults button, and apply-on-Enter for number inputs. Responsive pass: sticky timer visual at the top of the view, leaf-level horizontal scrolling on narrow sidebars (controls + settings + tasks lock at 260px and never shrink), day/night indicator auto-fades when the side panel drops below 200px, clock floors at 200×200, leaf min-height held at 320px so the divider stops shrinking past usable size.

**0.1.3 added:** docs-only release for the Obsidian Community Plugins catalog launch. README install path moved from BRAT to Community Plugins, and the feature list now reflects the 0.1.1 (bundled audio, `prefers-reduced-motion`) and 0.1.2 (redesigned in-view settings panel, narrow-sidebar responsive layout) changes that had not yet been written up.

Plugin ID: `gentle-pomo`. Mobile-compatible. Min Obsidian: 1.0.0.

## Build & Dev Commands

```bash
npm install            # bootstrap
npm run dev            # rollup --watch (rebuilds main.js into project root)
npm run build          # one-shot production build
npm run lint           # ESLint (Obsidian recommended + typed @typescript-eslint rules)
npm run lint:fix       # auto-fix what's safe
npm run format         # prettier --write .
npm run format:check   # prettier --check . (used by CI)
npm test               # vitest run
npm run test:watch     # vitest in watch mode
```

CI in [.github/workflows/ci.yml](.github/workflows/ci.yml) runs lint + format:check + test + build on every push and PR. Release in [.github/workflows/release.yml](.github/workflows/release.yml) attaches `main.js`, `manifest.json`, `styles.css` to GitHub Releases on tag push.

## Architecture

State flows through a listener pattern:

```
GentlePomoPlugin (main.ts)
    │
    ├── TimerEngine ──emit(TimerState)──▶ GentlePomoView   (renders the visual timer)
    │                                  └▶ status bar in main.ts
    │
    ├── LogManager       (appends daily-log markdown lines, TTL cache for today's totals)
    └── GentlePomoSettingTab  (Obsidian PluginSettingTab)
```

- `TimerEngine` is the single source of truth for timer state. Every subscriber gets a fresh snapshot via `onChange`.
- The view does **not** own state — it reads from `TimerEngine` and dispatches user actions back to it.
- Persistence is Obsidian's `loadData()`/`saveData()`; defaults are merged from `DEFAULT_SETTINGS` in [constants.ts](constants.ts).

## Source Map

| File                                               | Purpose                                                                                                                                                                                                           |
| -------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| [main.ts](main.ts)                                 | Plugin entry. Commands, ribbon, status bar, auto-open (via `onLayoutReady`), settings load/save.                                                                                                                  |
| [TimerEngine.ts](TimerEngine.ts)                   | Timer state machine: start/pause/finish/skip, mode switching, sound, task linking, vault-modify reactions.                                                                                                        |
| [GentlePomoView.ts](GentlePomoView.ts)             | Sidebar `ItemView` (~400 LOC). Builds DOM, subscribes to `TimerEngine`, renders task dropdown and day/night gradient.                                                                                             |
| [GentlePomoSettingTab.ts](GentlePomoSettingTab.ts) | Settings page (folder paths, auto-open, status bar, day/night toggle).                                                                                                                                            |
| [logManager.ts](logManager.ts)                     | Writes daily `*-gentle-pomodoro-log.md` files, tracks pauses, refreshes logged task names by ID, caches today's focus total.                                                                                      |
| [taskLoader.ts](taskLoader.ts)                     | Parses Tasks-plugin markdown (`⏳ 📅 🆔 🔺`), normalizes display text, groups by date. Per-task counter helpers: `parseTodayPomodoroCount` / `incrementTodayPomodoroCount`.                                       |
| [icons.ts](icons.ts)                               | Inline SVG day/night icons (`buildDayNightIcon`, `DAY_NIGHT_ICON_ORDER`).                                                                                                                                         |
| [logger.ts](logger.ts)                             | `logger.warn/error/debug` — auto-prefix `[GentlePomo]`. Use instead of `console.*`. (`console.log` is disallowed by the lint config.)                                                                             |
| [types.ts](types.ts)                               | `PomoMode`, `GentlePomoSettings`, `TimerState`, `TimerListener`, `TaskItem`.                                                                                                                                      |
| [constants.ts](constants.ts)                       | `VIEW_TYPE_GENTLE_POMO`, `NO_TASK_LABEL`, `ONE_MINUTE_MS`, `FOCUS_TOTAL_CACHE_TTL_MS`, `DEFAULT_SETTINGS`. New settings live here as defaults (long-break, daily-goal, per-task counter, transient state fields). |
| [momentTypes.ts](momentTypes.ts)                   | Type stubs for Obsidian's bundled `moment` global.                                                                                                                                                                |
| [styles.css](styles.css)                           | All visual styling. Class names are `gp-*` prefixed. Responsive via `clamp()` and container queries. Two themes scoped via `.gp-theme-classic` / `.gp-theme-frosted-glass` on `.gp-root`.                         |
| [THEMES.md](THEMES.md)                             | Documents the two shipped themes (Classic, Frosted Glass) and four future theme ideas (Moon Phases, Tide, Zen Ripple, Lantern) with the wiring pattern for adding a new theme.                                    |
| [CHANGELOG.md](CHANGELOG.md)                       | Per-version change log (Keep a Changelog format). Canonical version history — the "X.Y.Z added" snippets at the top of this file are for at-a-glance context.                                                     |
| [tests/](tests/)                                   | Vitest unit tests for taskLoader, logManager (formatLogLine + parseFocusTotalSeconds), TimerEngine state machine.                                                                                                 |
| [\_\_mocks\_\_/obsidian.ts](__mocks__/obsidian.ts) | Minimal Obsidian-API stubs so unit tests can `import` from project files without an Obsidian runtime.                                                                                                             |

## Conventions

- **State updates flow through `TimerEngine.emit()`** → listeners. Never mutate `TimerState` from the View or status bar.
- **Defaults live in [constants.ts](constants.ts)** `DEFAULT_SETTINGS`. New settings must be added there and merged via `Object.assign` in `loadSettings()` ([main.ts](main.ts) `loadSettings`).
- **Task IDs** are detected via the `🆔` emoji marker. Log entries use Obsidian wiki-link syntax: `[[path|display name]]`.
- **CSS classes are `gp-*` prefixed.** Stay consistent — search [styles.css](styles.css) before inventing new names.
- **Logging:** use `logger.warn/error/debug` from [logger.ts](logger.ts), not raw `console.*`. The prefix `[GentlePomo]` is added automatically.
- **Audio assets** (`ding-sound.mp3`, `singing_bell_short.mp3`, `war-drum_short.mp3`) live at the project root and are loaded by relative path from [TimerEngine.ts](TimerEngine.ts).
- **Task parsing** must go through [taskLoader.ts](taskLoader.ts) (`loadTasks`, `groupTasksByDate`, `normalizeTaskText`, etc.) — never re-implement regex inline.

## Settings & Persistence

`GentlePomoSettings` in [types.ts](types.ts) is the schema. Stored by Obsidian in `data.json` at the project root. Modify via `plugin.settings.foo = ...` then `await plugin.saveSettings()`.

## Log Format

Daily file: `<folder>/YYYY-MM-DD-gentle-pomodoro-log.md`. One line per session, inline-field format so Dataview can query it:

```markdown
- 🍅 Focus | Task:: [[Projects/MyProject.md|Write Docs]] | ID:: abcd12 | Start:: 2025-12-23 10:00:00 | End:: 2025-12-23 10:25:00 | Scheduled:: 1500 | Pauses:: [] | Total:: 1500 | Status:: finished | Type:: focus
- ☕ Rest | Start:: 2025-12-23 10:25:00 | End:: 2025-12-23 10:30:00 | Scheduled:: 300 | Total:: 300 | Type:: short-break
- ☕ Rest | Start:: 2025-12-23 11:00:00 | End:: 2025-12-23 11:15:00 | Scheduled:: 900 | Total:: 900 | Type:: long-break
```

`Type::` was added in 0.1.0 — values are `focus | short-break | long-break`. Additive; existing Dataview queries still match.

Field order is **load-bearing** for users' Dataview queries — do not reorder or rename without a version bump and changelog note. The schema is locked in by [tests/logManager.test.ts](tests/logManager.test.ts).

## Important Quirks

- **`obsidian` dep is pinned to `^1.13.1`** (types-only npm package; 1.13 typings are required for the declarative settings API — `getSettingDefinitions()` in [GentlePomoSettingTab.ts](GentlePomoSettingTab.ts), with the imperative `display()` kept as the pre-1.13 fallback; keep both paths in sync when changing settings). [package.json](package.json) and [manifest.json](manifest.json) track 0.4.4; `minAppVersion` is **1.7.2** (raised from 1.0.0 for `Workspace.revealLeaf` / `Vault.createFolder`, which `eslint-plugin-obsidianmd`'s `no-unsupported-api` rule gates). [versions.json](versions.json) maps each plugin version to its `minAppVersion`.
- **Audio lifecycle:** [TimerEngine.ts](TimerEngine.ts) owns a single shared `AudioContext`, created lazily in `playSound()` and resumed if suspended; decoded clips are cached in `audioBuffers`. `TimerEngine.dispose()` (called from [main.ts](main.ts) `onunload`) closes the context and clears the tick loop — Chromium caps live `AudioContext`s, so it must not leak across disable/enable cycles.
- **`main.js` is the built artifact.** It's gitignored, but Obsidian loads it directly — you must `npm run build` (or have `npm run dev` running) before reloading the plugin.
- **iPad icon sizing needs a CSS `min-width` floor.** WebKit/Safari collapses an SVG flex item's main-axis (width) to ~min-content (~8px) while honoring height, so the control glyphs render as thin slivers — and a plain CSS `width` (even `!important`) does **not** fix it. The fix is a `min-width`/`min-height` floor (+ `flex-shrink: 0`) on `body.is-mobile/.is-tablet .gp-icon-btn svg.svg-icon` in [styles.css](styles.css); the `min-*` floor is the load-bearing part (the flex algorithm can't shrink below it), so don't drop it. It **must** be CSS, not inline `el.style.setProperty` — `eslint-plugin-obsidianmd`'s `no-static-styles-assignment` forbids inline style assignment (and disabling that rule). (Diagnosed device-side via Safari Web Inspector; desktop/iPhone were never affected.)
- **Auto-open** runs inside `this.app.workspace.onLayoutReady()`. A `MutationObserver` fallback waits for any settings/community-plugin modal to close before activating the view (handles freshly-installed-plugin UX).
- **`noImplicitOverride: true`** is enabled in [tsconfig.json](tsconfig.json). Always mark methods that override Obsidian base classes (`Plugin.onload`, `ItemView.onClose`, etc.) with `override`.
- **`package-lock.json` is tracked** — CI uses `npm ci` for reproducible builds (since 0.1.1). Regenerate with `npm install` whenever `package.json` changes.
- **Local-timezone date string** used for daily reset logic is `moment().format("YYYY-MM-DD")` (defined as `todayLocalStr()` in both [main.ts](main.ts) and [TimerEngine.ts](TimerEngine.ts)). Same format as the daily log file naming, so all "today" logic agrees.
- **Per-task counter is opt-in** (`incrementPomodoroCountOnFinish`, default off). When enabled, finishing a focus session linked to a task increments a lifetime `🍅 N` marker on the task line. Legacy `🍅 N (YYYY-MM-DD)` markers from an earlier 0.1.0 build are still readable; on the next increment the parens are stripped and the marker becomes `🍅 N+1`.
- **Internal state fields** in `GentlePomoSettings` (`lastGoalHitDate`, `sessionsSinceLongBreak`, `sessionCounterDate`) are persisted to `data.json` alongside user settings but are not surfaced in any settings UI — they implement the once-per-day notice and long-break counter.

## Don't Do This

- Don't add `setInterval` calls in the view — subscribe to `TimerEngine.onChange` instead.
- Don't touch the DOM from `TimerEngine` or `LogManager` — UI rendering belongs in the View.
- Don't reach into `this.app.metadataCache` for task parsing — use [taskLoader.ts](taskLoader.ts).
- Don't use `console.*` directly — go through [logger.ts](logger.ts) (and `console.log` is lint-blocked anyway).
- Don't reorder or rename inline fields in log lines — tests in [tests/logManager.test.ts](tests/logManager.test.ts) will fail, and users have Dataview queries pinned to the current schema.

## Reusable Patterns

- **Listener pattern:** `onChange/offChange/emit` in [TimerEngine.ts](TimerEngine.ts) — copy this shape for any new subscribable state.
- **Settings merge:** `Object.assign({}, DEFAULT_SETTINGS, loaded ?? {})` in [main.ts](main.ts) `loadSettings`.
- **TTL cache:** `FOCUS_TOTAL_CACHE_TTL_MS` + `lastFetchMs` + `fetchInFlight` pattern in [main.ts](main.ts) `maybeRefreshFocusTotal` and [logManager.ts](logManager.ts) `getTodayFocusSeconds` — reuse for any expensive periodic fetch.
